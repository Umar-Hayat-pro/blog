<div>
  <h1><%= @article.title %></h1>
  <p><%= @article.body %></p>
</div>
<ul>
  <li><%= link_to "Edit", edit_article_path(@article) %></li>
  <li><%= button_to "Destroy", article_path(@article), method: :delete %></li>
</ul>
<div>
  <h6>Make a comment</h6>
  <%= render 'comments/form' %>
  <%= render 'comments/see-comments' %>